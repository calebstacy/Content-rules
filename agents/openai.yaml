interface:
  display_name: "Content Rules"
  short_description: "Turn exact content guidance into tested checks"
  default_prompt: "Use $content-rules to extract exact rules from my content guidance, configure the shipped checks, and self-lint governed content until exact checks pass or human review is required."
