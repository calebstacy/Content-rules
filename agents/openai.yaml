interface:
  display_name: "Content Rules"
  short_description: "Turn exact content guidance into contextual checks"
  default_prompt: "Use $content-rules to extract exact rules from my content guidance, preserve any explicit situations or exceptions as typed contextual facts, configure the shipped checks, and self-lint governed content until exact checks pass or human review is required."
